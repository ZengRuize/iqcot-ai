# Codex Next Action Recommendation

Date: 2026-07-01
Branch: codex/rigorous-iqcot-review-git-managed

Do not run new simulations immediately.

First:

```text
complete language audit
complete problem-solution matrix
complete manuscript evidence package
```

Then:

```text
if more validation is needed, run E020 settling audit
```

Do not start:

```text
E020-R2 tuning without settling audit
A5-R4 projected scheduling
broad load grids
active Lambda validation
active-phase broad scheduling
hardware claims
```

The next Codex task should build the manuscript evidence package: one table of action-token claims, one table of experiment evidence and boundaries, and one figure plan with traceable source metrics.
